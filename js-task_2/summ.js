const q = 10; // Пункт 5
      u = 2;

let s = q + u;
console.log(s);

let c = q - u;
console.log(c);

let p = q * u;
console.log(p);

let r = q / u;
console.log(r);

if (s > 1) {
    n = s * s ;
    console.log(n);
}

let mes = (q > 2 && q < 11) ? 'верно' : (u > 6 && u < 14) ? 'верно' : 'неверно' ; // Пункт 6
console.log(mes);
